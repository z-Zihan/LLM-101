# V3.1 中央闭环 B3 审计

> 批次：Prefill、Decode、Sampling 与 KV Cache · 日期：2026-08-21

## 交付范围

| 主页面 | 本批补齐的闭环 | 回收真实问题 |
|---|---|---|
| [为什么 ChatGPT 的字是一个一个蹦出来的](./docs/03-模型原理与训练/14-为什么ChatGPT的字是一个一个蹦出来的.md) | Prompt → Tokenizer → Prefill → KV Cache → Logits → Greedy / Sampling → New Token → Context → Decode Loop → Stop | Token 生成、Temperature、输出限制、KV Cache、Prefill / Decode |
| [训练和推理有什么区别](./docs/03-模型原理与训练/18-训练和推理有什么区别.md) | 保留“训练更新参数、推理增长请求状态”的比较，把生成策略与缓存细节移交唯一 Decode 主页面 | 训练循环、推理随机性、推测解码 |
| [GPU、显存和推理瓶颈](./docs/03-模型原理与训练/19-GPU显存和推理瓶颈.md) | 继续承载 KV Cache 显存公式、TTFT、Inter-Token Latency 与输入 / 输出 Throughput 的高级性能解释 | `q-r2-gpu-001` 至 `q-r2-gpu-008` |

本批复用现有 8 条直接相关真实问题，没有新增编辑伪问。牛客公开来源中的 `q-r2-gpu-005` 至 `008` 增加面试来源元数据；Reddit 来源的 `q-r2-gpu-004` 仍按公开社区问题记录，不错误标成面试题。

## 关键纠偏

- 聊天界面逐“字”显示只是视觉效果；模型生成单位是 Token，传输层还可能合并多个 Token。
- Prefill 与 Decode 都是推理；前者处理已知输入并建立缓存，后者依赖上一步结果逐 Token 生成。
- 第一个输出使用完整 Prompt 最后有效位置的表示产生 Logits，不是一次算出完整回答。
- Greedy 与 Sampling 是候选选择策略，不分别代表“理性”与“创造力”。
- Temperature 缩放 Logits，不修改参数或知识；`temperature = 0` 通常由 API 作为特殊贪心分支处理。
- Top-k 保留固定候选数，Top-p 保留累计概率达到阈值的动态集合；运行时可能组合多种过滤规则。
- KV Cache 保存每层历史位置的 K/V 状态，不保存长期自然语言记忆，也不能消除自回归串行依赖。
- EOS 只是停止条件之一；输出上限、Context Window、Stop Sequence、取消和超时也能终止生成。
- 输出上限与上下文窗口不同；具体服务还可能分别限制输入、输出和总量。
- Prefill Latency、Decode Tokens/s、单请求速度和服务器总吞吐必须分开报告。

## 十六项 Review

| Review | 结果 | 核验摘要 |
|---|---|---|
| Accuracy | PASS | 温度公式限定 `T > 0`，Top-p 定义、因果串行、K/V 缓存、停止条件与性能口径均有边界 |
| Beginner | PASS | 从“中国的首都是”逐步走到“北京”，先建立完整循环再解释术语 |
| Beginner Depth | PASS | 覆盖 Prompt 组成、首位置 Logits、候选过滤、缓存增长、停止与延迟 / 吞吐 |
| Chinese Language | PASS | 中文承担过程说明，Prefill、Decode、EOS 等术语首次出现即解释职责 |
| Real Question | PASS | 复用已有 8 条提问；4 条牛客面经与 1 条 Reddit 性能问题的来源类型保持真实 |
| Question Coverage | PASS | Token 生成、逐 Token 原因、Temperature、Top-k / p、KV Cache、输出限制和性能均回收 |
| Knowledge Graph | PASS | 新增 10 个节点，105 个节点与 169 条语义关系通过双向校验 |
| Cross-link | PASS | Decode 页连接 Token、Next-token、Transformer、Loss、训练 / 推理、Context 与 GPU 页面 |
| Layout | PASS | 主链、分支选择、缓存与时间轴分别用小型流程图表达，没有用表格替代叙事 |
| AI Writing Smell | PASS | 以一次请求的状态变化推进，避免百科式术语堆叠与机械模板复用 |
| Architecture | PASS | 新增一个 Decode 唯一主页面，训练 / 推理页只保留阶段比较，GPU 页只承载性能深挖 |
| Terminology | PASS | Tokenizer Decode 与服务阶段 Decode 明确区分；Context、KV Cache、Parameter 状态不混用 |
| Duplication | PASS | 旧页的 Temperature、KV Cache 与 Prefill / Decode 细节已收敛成摘要链接 |
| Source | PASS | 使用 Transformer、Nucleus Sampling、PagedAttention 原始论文及 HF / NVIDIA / PyTorch 官方资料 |
| Link | PASS | 全量 171 个 Markdown 文件相对链接通过；4 条牛客面经和论文 / NVIDIA 页面返回 HTTP 200 |
| End-to-End Mechanism | PASS | 输入、前向、训练更新与推理解码三条链可连续追踪，v3.1 的 16 个机制问题均有正文答案 |

## LLM Core Mechanism Review

```text
Text → Tokenizer → Token → Token ID → Embedding
     → Transformer → Attention / MLP → Hidden Representation
     → Logits → Probability

Training: Correct Token → NLL / Cross Entropy → Loss
        → Backpropagation → Gradient → Optimizer → Parameter Update

Inference: Prompt → Prefill → KV Cache → Logits → Sampling
         → New Token → Context → Decode → Final Answer
```

逐项复述结果：Tokenize 解决输入单位，Token ID 用于查 Embedding；Attention 交换位置信息，MLP 逐位置加工；输出投影产生全词表 Logits。训练读取真实下一 Token，经 NLL / CE、反向传播和优化器更新并最终保存参数。推理加载固定参数，Prefill 处理输入，Decode 选择并追加 Token；Temperature / Top-p 只改变选择策略，KV Cache 避免重复计算历史 K/V。

## 批次数据

- Decode 正文 7,358 字符，落入 v3.1 建议深度区间；
- 真实问题保持 194 条，全部 answered；
- 知识节点：95 → 105；语义关系：152 → 169；
- `知识网络.md` SHA-256：`461dd4cbcba7a5b0e787e4d87796ee3936abcb5a2b43392190b708e0a8afe33c`；
- `真实问题矩阵.md` SHA-256：`ce533f66de51907f38364af6ad2ab362ad3ea48188bea9741530df8956f82197`。

两份生成物连续构建两次哈希一致；严格问题、概念、链接、Python 语法与 Git 差异检查全部通过后，本批方可提交。
