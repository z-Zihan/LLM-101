# LLM-101 中文教程排版与编辑规范

这份规范用于所有面向读者的页面。目标不是把文章排得整齐，而是让零基础中文读者能顺着问题自然读下去，并感觉内容经过人工编辑。

## 页面首先服务阅读

一篇文章只保留一个一级标题。二级标题对应真正的认知阶段；如果相邻两段本来能连续阅读，就不要为了“结构完整”再插入标题。三级标题只在同一阶段确实需要拆分时使用。

正常解释通常以 2～5 句组成一个自然段。关键转折可以单独成段，但连续出现许多一句话段落时，应重新合并。长段落如果同时承担定义、例子、边界和结论，也应拆开。

列表只用于明确并列、步骤、对比或检查项。表格只在读者需要横向比较时使用，不拿来承载大段解释，也不为了显得专业而每篇放一张。粗体只标真正影响理解的结论；同一段多次加粗通常意味着重点没有选好。

## 图示必须解决一个问题

ASCII、Mermaid 和简单层级图都可以使用。图前要说明“这张图想帮你看懂什么”，图后要指出“读图时最重要看哪里”。如果删掉图不影响理解，优先删掉；如果图里出现尚未解释的术语，先改图或补解释。

## 中文是默认语言

行业里长期使用的 AI、LLM、Token、Transformer、API、RAG、Agent、MCP、Skill、Codex、Claude Code 和 PyTorch 可以保留。其他专业词第一次出现时先给中文，再在括号中写英文或缩写，后文使用中文或通用缩写。

不要写成“Model 在 Inference 阶段根据 Context 逐 Token Generate”。应写成“模型在推理阶段会根据当前上下文，一个 Token 一个 Token 地继续生成”。文件名、目录、教学导航和问题矩阵同样中文优先。

## 同一套质量，不是同一套骨架

文章可以从一个误解、一段真实追问、一个完整例子或一次失败经历进入。追问数量、标题数量、是否使用表格和图示，都由当前概念决定。不要让每篇文章依次出现“定义—类比—误区—总结—五件事”。

开头使用的真实问题必须在正文中得到足够深的回答，接近结尾时自然回收。问题可以是教学入口，但正文仍应是连贯文章，不能退化成整篇问答列表。

## AI 写作味审查

每批提交前逐篇检查：

- 标题和段落是否与其他文章异常同构；
- 是否反复出现“先说结论”“简单来说”“本质上”“你只需要记住”；
- 是否频繁使用“不是 A，而是 B”或机械的三段排比；
- 是否把同一个结论在开头、正文和总结重复三遍；
- 是否全是一句一段、密集列表、过量粗体或装饰符号；
- 类比是否只制造画面，却没有解释输入、过程、输出和边界；
- 是否为了填满模板而加入空洞鼓励、无关历史或机械问句；
- 是否像百科条目或技术 README，而不像有人根据读者卡点认真讲解；
- 是否把转述的公开问题伪装成逐字引用；
- 是否出现未经来源支持的绝对判断。

任一问题明显存在时，不能只换几个词，应调整段落职责、教学顺序或例子后重新审读。

## 每批排版验收

完成最多 3 篇正文后，从手机宽度和桌面宽度各扫读一次 Markdown：标题不能过密，代码块和表格不能横向失控，链接文字要能说明去向，图和正文必须互相解释。最后对照真实问题库，确认开头的问题确实被回答，而不只是被提到。
