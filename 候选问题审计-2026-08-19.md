# 2026-08-19 候选真实问题审计

这份账本对应用户提供的 `llm-101-真实问题库-候选补充-2026-08-19.yml`。候选文件共有 52 条问题；它不是主库，只有完成去重、来源检查、概念映射和正文补答后，问题才会进入 [`真实问题库.yml`](./真实问题库.yml)。

## 判定口径

- **接收**：问题有清晰的真实来源、适合本项目，并有明确答案页；按批次完成正文后再入库。
- **合并**：来源或认知簇已被主库问题完整覆盖，不重复制造记录。
- **Deferred**：问题真实且有价值，但目前缺少专门概念节点与足够答案页；保留理由，不伪装成已回答。
- 来源标记 `A` 表示已通过 Stack Exchange / Stack Overflow / Hacker News 官方 API 读取页面正文；`B` 表示已核对候选字段、URL、标题或 URL slug，但当前环境访问正文被社区反爬限制。`B` 不是“已完成实时正文复核”。

最终计划为：47 条接收、2 条合并、3 条 Deferred。每批最多改 3 篇正文。

## 逐题结论

| # | 候选 ID / 问题焦点 | 结论 | 映射或理由 |
|---:|---|---|---|
| 1 | `cand-cn-agent-roadmap-001` 原生 API 还是 Agent 框架 | 已接收 · B1 · B | `q-agent-roadmap-v2ex-001` → `Agent到底是什么`；先看清调用与执行边界，再选择框架。 |
| 2 | `cand-cn-agent-roadmap-002` Agent 概念学习顺序 | 已接收 · B1 · B | `q-agent-concepts-roadmap-v2ex-001` → `Agent到底是什么`；映射函数调用、Agent、RAG、MCP、聊天记录。 |
| 3 | `cand-cn-agent-loop-001` 最小 Agent Loop | 已接收 · B1 · B | `q-agent-loop-v2ex-001` → `Agent-Loop是什么`；补足最小循环与生产护栏的区别。 |
| 4 | `cand-cn-rag-agent-001` Agent 是否必须有 RAG | 已接收 · B1 · B | `q-rag-agent-zhihu-001` → `RAG到底是什么`；RAG 与 Agent 是两条独立维度。 |
| 5 | `cand-cn-rag-long-context-001` 长上下文是否取代 RAG | 接收 · B2 · B | 目标 `RAG有哪些局限`；比较成本、更新、权限、可引用性和全量阅读。 |
| 6 | `cand-cn-rag-cleaning-001` 入库前清洗与结构化 | 接收 · B2 · B | 目标 `知识库是什么`；不把“切片”误当完整数据治理。 |
| 7 | `cand-cn-rag-chunking-001` Chunk 怎样切 | 接收 · B2 · B | 目标 `检索是什么`；按答案边界、结构、重叠和评测解释。 |
| 8 | `cand-cn-rag-recall-001` 召回率怎样优化 | 接收 · B2 · B | 目标 `检索是什么`；换 Embedding 只是可调环节之一。 |
| 9 | `cand-cn-rag-rerank-001` Rerank 解决什么 | 接收 · B2 · B | 目标 `检索是什么`；区分候选召回与候选内排序。 |
| 10 | `cand-cn-rag-sparse-dense-001` 稀疏、稠密与混合检索 | 接收 · B2 · B | 目标 `检索是什么`；映射 RAG、Embedding、检索。 |
| 11 | `cand-cn-vector-db-choice-001` 向量数据库选型 | 接收 · B3 · B | 目标 `向量数据库是什么`；先看规模、过滤、更新和运维，不按品牌答题。 |
| 12 | `cand-cn-rag-finetune-001` RAG 还是微调 | 合并 · B | 与 `q-rag-finetuning-web-001` 同一核心问法及答案页；保留候选来源记录，不在主库重复计数。 |
| 13 | `cand-cn-finetune-data-001` 微调数据集怎样构造 | 接收 · B3 · B | 目标 `RAG和微调有什么区别`；映射训练数据、训练、泛化。 |
| 14 | `cand-cn-finetune-eval-001` 微调后怎样评估 | 接收 · B3 · B | 目标 `RAG和微调有什么区别`；训练 Loss 不能替代独立任务评测。 |
| 15 | `cand-cn-transformer-gpt-001` GPT 与 Transformer | 接收 · B4 · B | 目标 `Transformer到底是什么`；区分架构家族与具体模型系列。 |
| 16 | `cand-cn-temperature-001` Temperature 改变什么 | 接收 · B4 · B | 目标 `训练和推理有什么区别`；只改变解码分布，不改参数知识。 |
| 17 | `cand-cn-output-limit-001` 输出上限与上下文窗口 | 接收 · B4 · B | 目标 `上下文和上下文窗口是什么`；说明输入与输出共用预算时的边界。 |
| 18 | `cand-cn-ai-memory-001` AI 记忆存在哪里 | 接收 · B5 · B | 目标 `聊天记录是什么`；拆分参数、上下文、外部存储与检索。 |
| 19 | `cand-cn-context-length-001` Context Length 由什么决定 | 接收 · B4 · B | 目标 `上下文和上下文窗口是什么`；架构、训练、推理实现共同约束。 |
| 20 | `cand-cn-dify-mcp-001` 平台与 MCP 协议 | 接收 · B6 · B | 目标 `MCP到底是什么`；产品平台可以使用协议，但二者不是同类。 |
| 21 | `cand-cn-db-access-001` RAG、函数调用、MCP 与 Text-to-SQL | 接收 · B6 · B | 目标 `AI工具到底是什么`；按知识检索、调用描述、连接协议和查询生成分工。 |
| 22 | `cand-cn-agent-browser-security-001` 浏览器 Prompt Injection | 接收 · B6 · B | 目标 `Agent-Loop是什么`；网页内容是不可信数据，不能获得指令优先级。 |
| 23 | `cand-cn-rag-exact-terms-001` 精确术语为何难搜 | 接收 · B2 · B | 目标 `检索是什么`；词法信号与语义信号互补。 |
| 24 | `cand-cn-rerank-limit-001` 未召回文档无法重排 | 接收 · B2 · B | 目标 `检索是什么`；明确 Rerank 的候选集上限。 |
| 25 | `cand-cn-rag-global-context-001` 跨章节与全局结构 | 接收 · B2 · B | 目标 `RAG有哪些局限`；保留 `advanced` 难度，说明层级检索与结构化状态。 |
| 26 | `cand-int-context-memory-001` 参数记忆与上下文记忆 | 接收 · B5 · B | 目标 `聊天记录是什么`；上下文满了不是参数被擦除。 |
| 27 | `cand-int-effective-context-001` 有效上下文长度 | 接收 · B4 · B | 目标 `上下文和上下文窗口是什么`；标称容量不等于每种任务的有效利用。 |
| 28 | `cand-int-positional-encoding-001` 为什么需要位置编码 | 接收 · B4 · B | 目标 `Transformer到底是什么`；Attention 计算需额外顺序信号。 |
| 29 | `cand-int-transformer-variants-001` Encoder / Decoder 变体 | 接收 · B4 · B | 目标 `Transformer到底是什么`；按可见信息与训练任务解释。 |
| 30 | `cand-int-rag-pipeline-001` 实用 RAG 管线 | 接收 · B3 · A | Stack Overflow 正文已核验；目标 `RAG到底是什么`。 |
| 31 | `cand-int-vector-irrelevant-001` 向量召回无关结果 | 接收 · B2 · A | Stack Overflow 正文已核验；目标 `检索是什么`。 |
| 32 | `cand-int-quant-big-small-001` 大 4-bit 与小 8-bit | 接收 · B7 · B | 目标 `参数到底是什么`；模型规模、量化误差、显存与运行速度要分别评测。 |
| 33 | `cand-int-quant-quality-001` 量化质量损失 | 接收 · B7 · B | 目标 `参数到底是什么`；不存在跨模型、任务和量化方法通用百分比。 |
| 34 | `cand-int-kv-cache-001` 为什么缓存 K/V 不缓存 Q | 接收 · B4 · A | AI Stack Exchange 正文已核验；目标 `上下文和上下文窗口是什么`。 |
| 35 | `cand-int-lora-qlora-001` LoRA 与 QLoRA | 接收 · B3 · B | 目标 `RAG和微调有什么区别`；高优先级入门问题，补参数高效微调边界。 |
| 36 | `cand-int-qlora-small-data-001` 约 1000 条样本是否够 | Deferred · B | 数量不能脱离任务、覆盖、噪声和评测回答；当前没有专项微调实践页，且候选优先级为 medium。 |
| 37 | `cand-int-dpo-001` DPO 分布偏移 | Deferred · A | 来源正文已核验，但需要 DPO、参考策略和偏好优化前置，超出当前知识节点与答案页。 |
| 38 | `cand-int-negative-sft-001` SFT 能否直接吃负样本 | Deferred · A | 来源正文已核验；需先建立 SFT、偏好数据与损失设计专题，不能塞进一段 FAQ 假装讲清。 |
| 39 | `cand-int-moe-basics-001` MoE 专家是什么 | 接收 · B7 · B | 目标 `模型架构是什么`；专家是可学习子网络，不是预先贴好的职业标签。 |
| 40 | `cand-int-moe-routing-001` Router 怎样选专家 | 接收 · B7 · B | 目标 `模型架构是什么`；路由通常是训练得到的计算模块。 |
| 41 | `cand-int-moe-active-001` 总参数与激活参数 | 接收 · B7 · B | 目标 `参数到底是什么`；区分每 Token 计算量与全部权重存储。 |
| 42 | `cand-int-distill-quant-001` 蒸馏与量化 | 接收 · B7 · B | 目标 `模型到底是什么`；一个改变训练所得模型，一个改变数值表示，可组合。 |
| 43 | `cand-int-speculative-quality-001` 推测解码是否降智 | 接收 · B4 · B | 目标 `训练和推理有什么区别`；区分严格采样算法与近似实现。 |
| 44 | `cand-int-prefill-decode-001` Prefill 与 Decode | 接收 · B4 · A | AI Stack Exchange 正文已核验；目标 `训练和推理有什么区别`。 |
| 45 | `cand-int-agent-memory-001` Coding Agent 持久记忆 | 接收 · B5 · B | 目标 `上下文工程是什么`；决策、证据和当前状态不能只靠压缩对话。 |
| 46 | `cand-int-long-context-memory-001` 长任务保存什么 | 接收 · B5 · B | 目标 `聊天记录是什么`；原文、摘要、检索与结构化状态各有职责。 |
| 47 | `cand-int-long-agent-001` 长时间 Agent 状态 | 接收 · B6 · B | 目标 `Agent-Loop是什么`；保留 `advanced` 难度，补检查点、外部状态与恢复。 |
| 48 | `cand-int-prompt-injection-tools-001` 工具让注入变成行动风险 | 接收 · B6 · B | 目标 `Agent-Loop是什么`；与 22 同簇但场景不同，保留两种真实问法。 |
| 49 | `cand-int-mcp-security-001` MCP 权限边界 | 接收 · B6 · A | Hacker News 主题与关联安全事件已核验；目标 `MCP到底是什么`。 |
| 50 | `cand-int-benchmark-001` Benchmark 污染 | 接收 · B8 · B | 目标 `怎么验证AI的回答`；补数据污染、榜单外推和私有任务集。 |
| 51 | `cand-int-inference-randomness-001` 推理可复现性 | 合并 · A | URL 与现有 `q-inference-randomness-web-001` 完全相同，问法也由同一来源转述，不重复入库。 |
| 52 | `cand-int-reasoning-think-001` `<think>` 怎样生成 | 接收 · B5 · A | AI Stack Exchange 正文已核验；目标 `推理能力是什么`，区分生成机制与能力解释。 |

## 批次状态

- [x] B1：Agent 学习路径、Agent Loop、RAG 与 Agent 边界（3 篇正文，4 条入库）
- [ ] B2：RAG 局限、知识库、检索
- [ ] B3：RAG 基础、向量数据库、RAG 与微调
- [ ] B4：Transformer、上下文窗口、训练与推理
- [ ] B5：聊天记录、上下文工程、推理能力
- [ ] B6：MCP、AI 工具、Agent Loop
- [ ] B7：参数、模型架构、模型
- [ ] B8：验证与全项目终审
