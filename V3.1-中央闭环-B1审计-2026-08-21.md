# V3.1 中央闭环 B1 审计

> 批次：文本编码、下一个 Token 预测与 Embedding · 日期：2026-08-21

## 交付范围

| 主页面 | 本批补齐的闭环 | 复用真实问题 |
|---|---|---|
| [模型不认识文字，那“你好”到底是怎么变成数字的](./docs/02-聊天Token与上下文/04-Token到底是什么.md) | Text → Tokenizer → BPE / Unigram → Vocabulary → Token → Token ID → Embedding Lookup | `q-token-length-web-001`、`q-token-generation-001` |
| [ChatGPT 看起来什么都会，底层只是在预测下一个 Token 吗](./docs/03-模型原理与训练/10-为什么预测下一个Token能学到能力.md) | Autoregressive LM → Next-token Prediction → Logits → Softmax → Probability；训练与生成分流 | `q-next-token-capability-web-001`、`q-loss-judgement-001` |
| [Token ID 怎样变成向量，Embedding 又为什么能用于检索](./docs/08-RAG与知识库/03-Embedding是什么.md) | 输入 Embedding、上下文化表示与检索 Embedding 的职责边界 | `q-token-generation-001`、`q-embedding-semantics-web-001` |

本批没有新增问题记录。现有原始聊天与公开问题已经直接覆盖 Token 边界、最终 Token 生成、下一个 Token 能力与 Embedding 语义；本批只把它们映射到更细的概念节点，避免复制同一问题。

## 关键纠偏

- Unicode / UTF-8 解决文字存取，Tokenizer 解决模型输入单位与 ID 映射，二者不是同一层编码。
- Token 不固定等于一个汉字、字符或单词；准确计数必须使用目标模型配套 Tokenizer。
- BPE 是常见子词词表构造方法，不是所有 LLM 的统一方案；SentencePiece 是支持 BPE、Unigram 等模型的工具包。
- Vocabulary 大小存在序列长度、输入 / 输出参数规模、稀有项训练量之间的权衡，不是越大越好。
- Token ID 是离散索引，编号距离没有语义；Embedding Lookup 才把 ID 转成可训练向量。
- 输入 Token Embedding、层内上下文化表示和 RAG 检索 Embedding 都是向量，但输出粒度、训练目标与用途不同。
- “下一个 Token 预测”只适用于本页讨论的主流自回归 LLM 主干，不能扩大成所有语言模型的唯一本质。
- 训练与聊天共享根据前缀计算 Logits 的前向过程；训练用真实目标更新参数，生成用解码策略选择下一项。

## 十六项 Review

| Review | 结果 | 核验摘要 |
|---|---|---|
| Accuracy | PASS | BPE、Unigram、SentencePiece、词表权衡、自回归与 Logits 边界均有 P0 / P1 支撑 |
| Beginner | PASS | 从“电脑已经能存字，为何模型还要切 Token”进入，并用逐步例子解释 |
| Beginner Depth | PASS | 回答到词表训练、未知组合、ID 查表、模型差异和训练 / 生成分流 |
| Chinese Language | PASS | 中文承担叙述，Vocabulary、Logits 等首次出现时说明职责 |
| Real Question | PASS | 复用 5 条已有真实问题，没有新增编辑伪问或伪造来源 |
| Question Coverage | PASS | Token 长度、BPE 边界、Token ID、Embedding 与下一 Token 循环均在结尾回收 |
| Knowledge Graph | PASS | 新增 10 个细粒度节点并完成问题双向映射，无孤立节点 |
| Cross-link | PASS | Token、Next-token、Embedding、Transformer、训练 / 推理之间建立入口 |
| Layout | PASS | 流程图解释转换，表格只用于训练 / 生成和三类 Embedding 比较 |
| AI Writing Smell | PASS | 三页分别采用编码链、生成循环和向量用途比较，无统一模板堆叠 |
| Architecture | PASS | 沿用 3 个唯一主页面，没有为 BPE、Logits 等机械拆短文 |
| Terminology | PASS | Text、Token、Token ID、Embedding、Logits、Probability 层次一致 |
| Duplication | PASS | Token 页给输入直觉，Embedding 页承担向量类型边界，机制定义不冲突 |
| Source | PASS | 使用 BPE、SentencePiece 原始论文及官方仓库、PyTorch 与 Tokenizers 文档 |
| Link | PASS | 全量 Markdown 相对链接检查通过 |
| End-to-End Mechanism | PASS | 零基础读者可从人类文本连续追到概率分布，并说清训练与生成下一步不同 |

## 批次数据

- 正文与图谱资产保持 86；真实问题保持 194，全部 answered；
- 知识节点：73 → 83；语义关系：130 → 141；
- `知识网络.md` SHA-256：`b00ad882adf4c46029648e7041af339d356580b70ce0120d190adfc5405594a8`；
- `真实问题矩阵.md` SHA-256：`0af62d93165cf0ac068cfb6c854cf8f110a97f0753de15a893abd1b92ceb00f5`。

严格问题、概念、链接、Python 语法、生成确定性和 Git 差异检查通过后，本批方可提交。
