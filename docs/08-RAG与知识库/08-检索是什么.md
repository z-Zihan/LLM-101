# 检索是什么？

> 所属专题：RAG 与知识库 · 前置：[向量数据库是什么](./05-向量数据库是什么.md) · 后续：[RAG 和微调有什么区别](./11-RAG和微调有什么区别.md)

RAG 面对几万份资料时，不会把全部内容塞进上下文。检索（Retrieval）接收一个查询，从候选集合中召回并排序可能相关的文档、片段或记录。它负责找证据候选，不生成最终答案，也不保证候选足够回答问题。

这层如果找错地区、版本或条款，后面的模型写得再流畅，也只能在错误证据上继续。

## 从问题到候选发生了什么

用户问“中国区员工今年出差住宿上限是多少”。系统可能识别主题、地区和时间，执行关键词与向量检索，用元数据过滤到中国区和有效版本，再返回若干候选片段。

```text
Query（查询）
  + 可搜索的文档、片段和记录
  ↓ 匹配、过滤、排序
Top-k 候选
  ↓ 重排与上下文组织
模型阅读的证据
```

这张图想区分候选和证据。读图时注意 `top-k` 只表示取分数较高的前 k 项；它不保证其中有正确答案，也不代表应该全部送入模型。

## 关键词与向量各擅长什么

关键词或稀疏检索擅长精确名称、编号和罕见词。用户输入法规编号、错误码或产品型号时，字面匹配非常有价值；缺点是不同措辞可能互相错过。

稠密检索使用 Embedding 比较查询与文档向量，更容易连接近义表达，却可能忽略数字、否定和细粒度条件。混合检索把两类分数和业务信号结合，不能保证自动变好，仍要用真实查询调权重与测试。

SQL、图查询和规则过滤也属于检索手段。数据本来就是结构化记录时，不必先把所有内容转成向量。

## Chunk 为什么改变结果

文档通常先切成 Chunk。片段太大，会混入无关信息并稀释重点；片段太小，可能把标题、适用条件和结论拆开。表格、列表和跨页段落还需要特殊处理。

切分要保留来源位置和层级，例如让“每晚 800 元”始终带上“中国区、一级城市、2026 年政策”的限定。只优化固定字符长度，很容易召回一个没有条件的数字。

因此 Chunk 不是越长或越短越好。说明文可以优先按标题和段落切，问答库可以围绕一组完整问答，代码要尊重函数或类边界，表格则要保留表头。适量重叠可以避免边界截断，却会制造重复候选并增加索引成本。最终尺寸、重叠和层级策略要用目标问题评测，而不是照搬某个框架的默认值。

## 召回率和精确率在拉扯什么

召回率关注需要的证据有没有被找回来；精确率关注找回来的候选里有多少真正相关。扩大 top-k 可能提高召回，也会增加噪声、延迟和上下文占用。

第一阶段常快速找回较多候选，再由 Rerank（重排）用更精细的模型重新比较查询与片段，选择少量内容进入上下文。重排能改善顺序，却无法救回第一阶段完全漏掉的文档。

这给出了一个很实用的边界：召回阶段决定“谁有资格参赛”，重排只决定这些候选“谁排前面”。如果正确文档不在候选集里，应检查查询改写、关键词通道、Embedding、Chunk、过滤条件和候选数量，而不是继续调重排器。

没有答案的查询也要测试。系统若总把最相似片段当答案，会在资料缺失时产生高置信错误；合理结果可能是“知识库没有足够证据”。

## 权限、时效和来源不是附加项

用户无权访问的资料应在检索前或检索过程中排除。把机密片段取回后再要求模型不泄露，数据已经越过边界。

相似度也不能处理所有时效和来源质量。有效日期、地区、文档状态和权威等级适合用元数据过滤或排序。最终引用仍要能回到原文。

排查错误时应分层：正确证据没进候选，是召回问题；进了候选却排名太低，是排序问题；进入上下文却被模型误读，是生成问题；资料本身缺失或错误，则要修知识库。

## 为什么只换 Embedding 往往不够

召回质量由多层共同决定。查询可能太短或带歧义；文档可能切坏；向量模型可能不适合语言和领域；相似度度量与向量归一化可能不匹配；元数据过滤可能误删正确版本；关键词通道、top-k 和融合权重也会改变结果。

排查时先准备一组“问题—应命中文档”样本，观察正确证据是否进候选以及排在第几。再逐项替换切分、查询、稀疏/稠密通道和模型。一次同时换掉整条管线，即使分数提高，也很难知道真正起作用的是哪一环。

专业缩写、型号、错误码和人名尤其容易暴露纯向量检索的边界。它们可能在语义空间里没有稳定邻域，却有很强的字面信号。BM25 等稀疏检索保留词项匹配，稠密检索连接不同说法；混合检索把两者合并，再让重排器处理候选。这里的“混合”不是固定公式，仍需在真实查询上校准。

向量库返回看似无关结果时，还要先确认一个容易忽略的事实：系统通常无论分数多低都会返回 top-k。若语料里根本没有答案，“最相近”也可能完全不相关。应查看原始分数分布、设置拒答或阈值策略，并加入无答案测试，而不是只看第一条结果的自然语言表面。

## 回答开头的问题

**检索找到的内容就是最终答案吗？** 不是，它是供后续生成和验证使用的候选证据。

**相似度最高就最有用吗？** 不一定。权限、时效、来源、数字和适用条件同样重要。

**top-k 越大越好吗？** 不一定。更多候选可能提高召回，也可能带来噪声与冲突。

**最终回答错了，一定是模型幻觉吗？** 不一定，也可能是知识库、切分、查询、过滤、召回或排序失败。

**RAG 召回率低，只换 Embedding 模型够吗？** 通常不够。模型只是查询、切分、索引、过滤、融合和候选数量中的一环；必须用有标准答案的查询集定位失效层。

**Rerank 解决召回还是排序？** 它主要改进已召回候选的排序。正确文档若未进入候选集，重排无法把它找回来。

**为什么精确术语常要混合检索？** 型号、缩写和编号依赖字面匹配，近义表达依赖语义匹配；稀疏与稠密通道提供互补信号。

## 从这里继续

- 比较另一种更新方式：[RAG 和微调有什么区别](./11-RAG和微调有什么区别.md)
- 看失败全景：[RAG 有哪些局限](./12-RAG有哪些局限.md)
- 回看完整流程：[RAG 到底是什么](./01-RAG到底是什么.md)
- 返回全局：[知识网络](../../知识网络.md) · [真实问题矩阵](../../真实问题矩阵.md)

## 资料与核验

- [Karpukhin et al.: Dense Passage Retrieval](https://arxiv.org/abs/2004.04906)
- [Lewis et al.: Retrieval-Augmented Generation](https://arxiv.org/abs/2005.11401)
- [Reimers & Gurevych: Sentence-BERT](https://arxiv.org/abs/1908.10084)
- [Thakur et al.: BEIR — A Heterogeneous Benchmark for Zero-shot Evaluation of Information Retrieval Models](https://arxiv.org/abs/2104.08663)
- [Sentence Transformers: Retrieve & Re-Rank](https://www.sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html)
